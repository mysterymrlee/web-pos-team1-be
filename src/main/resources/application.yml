spring:
  profiles:
    include:
      - db
      - import
      - sms
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
logging:
  level:
    com.example: debug
server:
  servlet:
    context-path: /